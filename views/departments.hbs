<div class="table-responsive">
    <div class="col-md-12 text-center">
        <h2>Employees</h2>
            <hr />
        <table class="table">
            <thead>
                <tr>
                    <th>Department</th>
                    <th>Name</th>
                </tr> 
            </thead>
            {{#each departments}}
                <tbody>
                    <td><a href="/employees?department={{departmentId}}">
                        {{departmentId}}</a></td>   
                    <td><a href="/employees?department={{departmentId}}">
                        {{departmentName}}</a></td>
            </tbody>
            {{/each}}
        </table>
    </div>
</div>
